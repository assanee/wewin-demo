import { SectionPlaceholder } from '@/components/section-placeholder';

/** SEAM — replace this file wholesale with the image library. */
export default function MediaPage() {
  return (
    <SectionPlaceholder
      titleTh="คลังรูปภาพ"
      descriptionTh="รูปสินค้าและรูปประกอบตัวเลือก"
    />
  );
}
