import { SectionPlaceholder } from '@/components/section-placeholder';

/** SEAM — replace this file wholesale with the option-group screens. */
export default function OptionGroupsPage() {
  return (
    <SectionPlaceholder
      titleTh="ชุดตัวเลือก"
      descriptionTh="กลุ่มตัวเลือกและค่าของตัวเลือก ที่สินค้าแต่ละเวอร์ชันหยิบไปใช้"
    />
  );
}
