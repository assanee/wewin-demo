{
  "extends": "./tsconfig.build.json",
  "compilerOptions": {
    "noEmit": true,

    /* Same split core and contract make: `dist` mirrors `src`, so the wider root that
     * lets the tests be type-checked lives here, where nothing is emitted. */
    "rootDir": "."
  },
  "include": ["src", "tests"]
}
